pipeline {
    agent any
    
    stages {
        stage('Setup build environment') {
            steps {
                echo "Installing poetry"
                echo "which python"
            }
        }
    }
}